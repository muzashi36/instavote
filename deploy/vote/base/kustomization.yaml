apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml

namespace: instavote

configMapGenerator:
  - name: vote
    literals:
      - OPTION_A=Summer
      - OPTION_B=Winter
